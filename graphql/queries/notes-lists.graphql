query NotesLists($collectionId: String!) {
  notesLists(collectionId: $collectionId) {
    id
    title
    createdAt
    updatedAt
    collection {
      id
    }
    notes {
      id
      title
      body
      createdAt
      updatedAt
    }
  }
}
